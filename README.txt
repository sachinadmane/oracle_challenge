1. The main URL for the application is at localhost:8000/login.

2. Upon successful authentication and login the user is directed to the page where the list of recipe's are displayed.

3. The structure and schema for the two mysql databases used in the application are attached with the project as well.

4. Users can be added by logging in through the django admin and creating users.